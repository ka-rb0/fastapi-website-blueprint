# Allowlist: only what the Dockerfiles COPY / bind-mount enters the build
# context - keeps .git, caches, node_modules and .devcontainer/.env (local
# secrets) out of every build.
*
!pyproject.toml
!uv.lock
!package.json
!package-lock.json
!src/
!src/app/
!src/app/**
src/app/**/__pycache__/
